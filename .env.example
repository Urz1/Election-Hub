# Database (PostgreSQL — e.g. Neon, Supabase, Railway)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Auth — generate with: openssl rand -hex 32
AUTH_SECRET=""

# Must match your deployed URL (no trailing slash)
NEXTAUTH_URL="https://your-domain.com"

# Resend (free tier: 3,000 emails/month) — https://resend.com/api-keys
RESEND_API_KEY=""

# Optional: custom sender email (requires verified domain in Resend)
# EMAIL_FROM="ElectHub <noreply@yourdomain.com>"

# Cloudinary (free tier: 25GB) — https://cloudinary.com/console
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=""

# Google OAuth — https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
